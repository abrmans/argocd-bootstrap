apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql-notarydatabase
spec:
  destination:
    namespace: harbor
    server: 'https://kubernetes.default.svc'
  project: default
  source:
    path: notaryserverdatabase
    repoURL: https://github.com/abrmans/postgresql
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
